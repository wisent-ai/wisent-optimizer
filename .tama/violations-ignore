# Written by the wisent-visuals banner bot. The banner below is rendered
# from .github/banner.toml and replaced whole on every run, so a repository
# audit reads it as generated output instead of asking somebody to split it.
assets/readme-banner.svg
#
# The repository root. Every file there is read by a tool at exactly that
# path: pip and setuptools read pyproject.toml and setup.py, GitHub reads
# README.md and LICENSE, the release check reads .wisent-release.json and
# released-surface.json, git reads .gitignore. None of them is a module that
# could move into a sub-folder, and the product code lives under wisent/.
.
